# Contributing to BYAN Frontend

- Fork repository and open a branch for your feature: `git checkout -b feat/my-feature`
- Write code, add tests where applicable.
- Run `npm install` and `npm run dev` locally.
- Create a pull request and describe your changes.

Be respectful and follow the code style. We use Prettier recommendations and basic ESLint rules can be added.
